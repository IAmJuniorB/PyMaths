from src import functions, numbers, utils

alg = utils.Algorithm()
con = utils.Constants()
seq = utils.Sequences()
fun = functions.MathFunctions()
hyp = functions.HyperbolicFunctions()
com = numbers.ComplexNumber()
ren = numbers.RealNumber(value=1)
rat = numbers.RationalNumber(numerator=1, denominator=1)
inn = numbers.IntegralNumber(value=1)
